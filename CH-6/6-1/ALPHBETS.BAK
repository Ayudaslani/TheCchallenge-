#include<stdio.h>
#include<conio.h>
void main(){
	char ch;
	clrscr();
	printf("Enter any character :");
	scanf("%c",&ch);

	if((ch >= 'A' && ch <= 'Z') || (ch >= 'a' && ch <= 'z'))
	{
		printf("%c is alphbets",ch);

	}
	else if(ch >= '0' && ch <= '9')
	{
		printf("%c is number ",ch);

	}
	else
	{
	     printf("%c is speical character ",ch);

	}
	getch();




}