#include<stdio.h>
#include<conio.h>
void main()
{
	int n;
	int rem;
	int rev = 0;
	clrscr();
	printf("Enter any number :");
	scanf("%d",&n);

	do
	{
		rem = n % 10;
		rev = rev * 10 + rem;
		n = n / 10;


	}while(n != 0);
	printf("the revser number is %d",rev);

	getch();

}