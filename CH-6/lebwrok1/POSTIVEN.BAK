#include<stdio.h>
#include<conio.h>
void main()
{
	int n;
	clrscr();
	printf("Enter any number :");
	scanf("%d",&n);

	if(n > 0)
	{
		printf("%d is positive number",n);
	}
	else if(n < 0)
	{
		printf("%d is nagative number",n);
	}
	else if(n == 0)
	{
		printf("%d is zero ",n);
	}
	else
	{
		printf("Invalid choice ");
	}
	getch();



}