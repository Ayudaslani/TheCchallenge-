#include<stdio.h>
#include<conio.h>
void main(){
	int a, b;
	clrscr();

	// maximum values
	printf("Enter a value of the first number :");
	scanf("%d",&a);
	printf("Enter a value of the second number :");
	scanf("%d",&b);

	if(a > b)
	{
		 printf("The maximum values is : %d",a);

	}
	else
	{
		printf("The maximun values is : %d",b);

	}
	getch();

}